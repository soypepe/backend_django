Para instalar primero se tiene que contar con python, luego se instala django con

`pip install django`

`django-admin startproject (nombre)`

Entrar en el directorio creado y ejecutar el entorno virtual

`pip install ..`
